<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.co.informatix.parkingviolationdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParkingViolationDemo">
        <activity android:name=".TimeConfirmActivity"></activity>
        <activity android:name=".UserInfoActivity" />
        <activity android:name=".VioStaticsActivity" />
        <activity android:name=".LabelActivity" />
        <activity android:name=".VioListActivity" />
        <activity android:name=".WizSeriesActivity" />
        <activity android:name=".WizMenuActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminModeActivity" />
        <activity android:name=".WizManagerActivity" />
        <activity android:name=".LoginUserActivity" />
        <activity android:name=".LoginTopActivity" />
    </application>

</manifest>